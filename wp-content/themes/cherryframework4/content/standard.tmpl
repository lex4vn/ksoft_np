%%THUMBNAIL%%
<div class="hp-label-info">
     <a class="hp-section-name" href="#">
     <?php $categories = get_the_category();
     if ( ! empty( $categories ) ) {
         echo esc_html( $categories[0]->name );
     }
     ?>
     </a>
</div>
%%TITLE where="loop"%%
<div class="hp-time-stamp timestamp__font">
	%%DATE%%
</div>
